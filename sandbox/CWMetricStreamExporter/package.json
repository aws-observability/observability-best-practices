{
    "name": "exporter",
    "version": "0.2.0",
    "scripts": {
        "build": "cd lambda && GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -o bootstrap main.go && cd ../cdk && cdk deploy"
    }
}
